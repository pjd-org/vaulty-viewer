/**
 * server/agent-shell/run-prompt-runner.ts
 *
 * Prompt Runner mode adapter entry point.
 * Re-exports promptRunnerAdapter for registration with the dispatcher.
 */

export { promptRunnerAdapter } from './prompt-runner-adapter';
